import TxtListPage from "@/components/pages/TxtListPage/TxtListPage";
import { FC } from "react";
import { TxtListScreenProps } from "./TxtListScreen.types";

const TxtListScreen: FC<TxtListScreenProps> = (props) => {
  return <TxtListPage />;
};

export default TxtListScreen;
